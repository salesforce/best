import { createElement } from 'lwc';
import HelloWorld from 'example/helloWorld';

describe('example-hello-world', () => {
    it('default dom', () => {
        expect('foo').toBe('foo');
    });
});
